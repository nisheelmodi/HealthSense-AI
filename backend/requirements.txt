# HealthSense AI — Backend Requirements
# Prompt 9: FastAPI / Uvicorn / Pydantic
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0

# Prompt 12: ML model inference dependencies
# These must match the versions used in ml/requirements.txt to avoid
# serialisation mismatches when loading the joblib-pickled artifacts.
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Prompt 16: Testing dependencies
pytest>=7.4.0
httpx>=0.24.0
